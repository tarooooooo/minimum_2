<% if target == "color" %>
  <% genres.each do |color| %>
    <%= form_with url: "/items/disposal", method: :get, local: true do |f| %>
      <%= f.hidden_field :color, value: color %>
      <%= f.hidden_field :category_id, value: category_id %>
      <%= f.submit color, class: "tbox-style border btn btn-light btn-lg btn-block" %>
    <% end %>
  <% end %>
<% elsif target == "brand" %>
  <% genres.each do |brand| %>
    <%= form_with url: "/items/disposal", method: :get, local: true do |f| %>
      <%= f.hidden_field :brand, value: brand %>
      <%= f.hidden_field :category_id, value: category_id %>
      <%= f.submit brand, class: "tbox-style border btn btn-light btn-lg btn-block" %>
    <% end %>
  <% end %>
<% elsif target == "price" %>
  <% genres.each do |price| %>
    <%= form_with url: "/items/disposal", method: :get, local: true do |f| %>
      <%= f.hidden_field :price, value: price %>
      <%= f.hidden_field :category_id, value: category_id %>
      <%= f.submit price, class: "tbox-style border btn btn-light btn-lg btn-block" %>
    <% end %>
  <% end %>
<% end %>