<div class="container">
  <div class="row mt-5">
    <div class="col">
      <h2>カテゴリー編集</h2>
      <div class="edit_cotegory">
        <%= form_with model: [:admin, @category], local:true do |f| %>
          <div class="form-group">
            <label for="category_name">カテゴリー名</label>
            <%= f.text_field :name %>
            <%= f.submit "保存", class:"btn btn-success"%>
          </div>
        <% end %>
    </div>
  </div>
</div>
